# Инструкция по деплою обновления профиля

## Шаги для применения изменений:

### 1. Залить код на сервер
```bash
# Если используете git
git add .
git commit -m "Update profile display format and buttons"
git push

# На сервере
git pull
```

### 2. Очистить кеш Laravel
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

### 3. Перезапустить PHP-FPM (если нужно)
```bash
# Для systemd
sudo systemctl restart php-fpm

# Или для других систем
sudo service php-fpm restart
```

### 4. Проверить, что изменения применены

#### Проверка кода:
```bash
# Проверить, что кнопки с URL поддерживаются
grep -A 5 "url.*button" app/Services/TelegramService.php

# Проверить формат профиля
grep -A 10 "Редактировать профиль" app/Http/Controllers/TelegramWebhookController.php
```

#### Тестирование в боте:
1. Напишите боту в **личном чате** команду `/profile`
2. Должен отобразиться профиль с:
   - ID
   - Имя
   - Ник в игре
   - Username
   - Рейтинг в боте
   - MMR
   - Dotabuff (кликабельный ник)
   - Настройки
   - **Две кнопки**: "Редактировать профиль" и "Перейти к боту"

3. В **группе** напишите `/profile`
   - Должен показаться только краткий формат: `**Ник** 🟤 Рекрут 1⭐`
   - Без кнопок, без полной информации

### 5. Если всё ещё не работает

#### Проверить логи:
```bash
tail -f storage/logs/laravel.log
```

#### Проверить, что файлы обновлены:
```bash
# Проверить дату изменения файлов
ls -la app/Http/Controllers/TelegramWebhookController.php
ls -la app/Services/TelegramService.php
```

#### Принудительно очистить весь кеш:
```bash
php artisan optimize:clear
```

### 6. Важные изменения в коде:

1. **TelegramWebhookController.php**:
   - Добавлен ID в профиль
   - Добавлен "Ник в игре"
   - MMR вместо "Очки"
   - Кликабельный ник Dotabuff
   - Кнопки с URL вместо callback_data

2. **TelegramService.php**:
   - Метод `sendMessageWithButtons` теперь поддерживает кнопки с `url`

### 7. Если кнопки не работают

Проверьте, что в `TelegramService.php` метод `sendMessageWithButtons` правильно обрабатывает URL:

```php
if (isset($button['url'])) {
    $buttonData['url'] = $button['url'];
} elseif (isset($button['callback_data'])) {
    $buttonData['callback_data'] = $button['callback_data'];
}
```

Если этого кода нет, значит файл не обновлен на сервере.

# 🔧 Решение проблемы с Composer на TimeWeb

## Проблема
При выполнении `composer install` возникает ошибка:
```
laravel/framework v10.50.0 requires composer-runtime-api ^2.2 -> no matching package found
```

Это означает, что версия Composer на сервере слишком старая.

## Решение 1: Обновить Composer (рекомендуется)

Выполните в терминале на сервере:

```bash
# Проверить текущую версию
composer --version

# Обновить Composer до последней версии
composer self-update

# Или обновить до конкретной версии
composer self-update 2.7.0
```

После обновления попробуйте снова:
```bash
cd ~/public_html/bot
composer install --no-dev --optimize-autoloader
```

## Решение 2: Использовать локальную версию Composer

Если обновление через `composer self-update` недоступно, установите локальную версию:

```bash
cd ~/public_html/bot

# Скачать последнюю версию Composer
php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php composer-setup.php
php -r "unlink('composer-setup.php');"

# Использовать локальную версию
php composer.phar install --no-dev --optimize-autoloader
```

## Решение 3: Использовать альтернативный путь к Composer

На некоторых хостингах доступна более новая версия Composer по другому пути:

```bash
# Попробовать разные пути
/usr/local/bin/composer install --no-dev --optimize-autoloader
/usr/bin/composer2 install --no-dev --optimize-autoloader
~/.composer/vendor/bin/composer install --no-dev --optimize-autoloader
```

## Решение 4: Понизить версию Laravel (если другие не работают)

Если обновление Composer невозможно, можно временно понизить версию Laravel в `composer.json`:

Измените строку:
```json
"laravel/framework": "^10.10"
```

На:
```json
"laravel/framework": "^10.0"
```

Затем удалите `composer.lock` и выполните:
```bash
rm composer.lock
composer install --no-dev --optimize-autoloader
```

## Проверка после установки

После успешной установки зависимостей:

```bash
# Создать .env файл
cp .env.example .env

# Сгенерировать ключ
php artisan key:generate

# Выполнить миграции
php artisan migrate --force

# Добавить вопросы
php artisan db:seed --class=Dota2QuestionsSeeder
```

## Дополнительная помощь

Если проблема сохраняется:
1. Проверьте версию PHP: `php -v` (должна быть >= 8.1)
2. Проверьте права доступа к папкам
3. Обратитесь в поддержку TimeWeb для обновления Composer
